# ORCHESTRATOR LOOP — living state (rewrite every cycle; this file survives compaction)

Standing orders (owner, 2026-09-03): work indefinitely; up to 8 subagents (Opus for
judgement, Sonnet for precisely-specified mechanical work); subagents report ONCE, briefly,
at the end, never mid-work; orchestrator reads minimally; checkpoint stable bot-design
states as `stable/*` branches on both repos; use self-play (deterministic runner +
`scripts/ab-compare.js`, per board class, never pooled) as the sanity gate for every
architectural change against the stable baseline, but do NOT over-optimise against it
(it lacks strategy diversity); pursue any factoring that deletes special cases in favour
of parameterised powerful abstractions; a recurring 55-minute ping keeps the cache warm;
nothing goes to `develop`/`main` directly (PRs #17 Chris-Centaur, #24 TacticToes).

## Baselines
- Chris-Centaur `stable/one-engine-lens-v5` = 1ca19d0 (97 suites / 1668 tests; 0 inversions on 16 arms; A/B vs v4 per class: mixed deaths 0.43→0.30 and edge 0, snakes/sparse byte-identical, potions 60t deaths down on 3/4 and on seeds 1–3; carries the honest contest floor, the peril-cache and horizon fixes, and the re-vendored engine with the outcome bracket). Older: v4 = bab7238, v3 = 90418c4, v2 = 439bec4, v1 = 514e1c6.
- TacticToes `stable/one-engine-v5` = 38c60f1 (claim tier interval brackets the frozen tier; crowded-board capacity error; 21 suites / 358 tests; goldens byte-identical). Older: v4 = 7483724. Older: v3 = a20c075, v2 = 526ff4f, v1 after the presence/grammar fix.
- Runner recordings for A/B: `node dist/tests/local-game.js <mixed|snakes|sparse|potions> 30 <seed> --nodes --json=F`; compare with `node scripts/ab-compare.js base.jsonl new.jsonl`.
- Inversion gate, 16 arms, every one must print no `INVERSION` line: `for s in mixed snakes sparse potions; do for d in 1 2 3; do CENTAUR_DEBUG_INVERSION=1 node dist/tests/local-game.js $s 30 $d --nodes; done; done` and then `for d in 4 5 6 8; do CENTAUR_DEBUG_INVERSION=1 node dist/tests/local-game.js potions 60 $d --nodes; done` — the four potion arms run to sixty turns because the two soundness classes fixed in 6e7b7d0 (a death in the optimistic timeline read as a proof; `room`'s maximised side read off a crowd containing a held cloud) fire on seeds 5 and 8 only past turn thirty, where the twelve 30-turn arms never looked.
- Opponent baseline (`--opponent`, against a NON-mirror field — see `resolveOpponent` in local-game.ts): `node dist/tests/local-game.js sum mixed,snakes,potions 30 3 --nodes --opponent=material-only --json=vs-material-only.jsonl --label=vs-material-only`; team 0's meals/100↑deaths/100 vs the seed-1-3 mirror baseline: mixed 7.78→13.17 / 0.00→1.23, snakes 18.33→18.33 / 0.00→0.00, potions 9.96→11.02 / 0.40→1.22 — a material-only field neither seeks food (weight 0) nor avoids a contest the way the default profile does, which is why team 0 eats more and dies more against it than it does mirroring itself.

## Active wave (update on every launch/merge) — 2026-09-05 06:07Z
Merged: BEHAVIOUR-AUDIT-2 (33c2b23: since audit 1 edge deaths 3→0, mixed deaths 10→6 with meals −8.8%, potions 26→21 with longestPark 20→44, reckless pickups 59→71%; P1 boxed pawn, P2 diluted collector exposure, P3 hunger in tanks, P4 = D5 unchanged). State: bot head = the working branch tip (see `git log -1`); `stable/one-engine-lens-v5` = 1ca19d0; TacticToes `stable/one-engine-v5` = 38c60f1. All three bot simplification plans and both TacticToes plans are fully executed. Both correctness reviews (docs/design/REVIEW-1.md on the bot, docs/REVIEW-1.md on TacticToes) are fully worked: every finding is fixed with a test or closed with evidence, except the TacticToes deploy hazard (owner's decision). Bot F3 taken (gate reads base; byte-identical); F2's repair measured and declined (safe in direction; tightening cost mixed contest deaths).
Since v5 on the bot (all gated, no decision moved unless stated): capof (terminal.ts reads the engine's OutcomeBracket; byte-identical), plan-2 item 7 (one sweepLists for B0–B3's leaves; byte-identical; gate:exact 16/16), review-2 (F1 family counters, F5 lens anchor dedup, F8 shutdown deadline; F4/F7/F9/F10 not defects; F6 a comment), engine re-vendored twice (outcome bracket; claims tier width off the schedule) with 16 arms zero after each.
Behaviour audit tally: audit-2 P1 REFUTED (masked mobility indicator: pawn parked 46→34% on potions with deaths 21→17 and D2's piece body deaths gone, but mixed deaths 6→7 and 8→9, all contest — a freed pawn crosses the board and `contest` prices arrivals not standing exposure; counters immobileUnitTurns/deathsWhileImmobile kept), P2 REFUTED (γ = 1/2, 1/3: reckless 71→71→67%, pickups 35→31→24, deaths 21→22→30; perilOf reads the ground from where the collector STANDS so the charge is the same on every collecting plan and cancels in the comparison — the next potion attempt must price the plan's own cell), P3 REFUTED (HUNGER_SPAN: sparse-lean meals 6.25→9.01/100 but deaths 0→1 — a full unit walked a two-wide dead end for a meal; other classes byte-identical only when written (1−e/cap)·(cap/span), an ulp lesson recorded); D1 TAKEN (honest contest floor: mixed deaths 10→9, potions 26→24, edge 0, contest.lo class closed); D2/D3/D4 REFUTED with mechanisms beside the code; D5 recorded; D6 fixed. Do not re-derive D2/D3/D4 from their predictions.
Lessons: the SESSION rate limit (HTTP 429, resets on the hour) kills every Opus worker at once — do not relaunch before the reset; container restarts kill every worker — checkpoint-and-push after every step is what makes both cheap; a dead agent cannot be resumed by SendMessage, relaunch from its branch; Sonnet briefs must say "never pass run_in_background, never call Monitor, never wait for a notification"; NO `git stash` anywhere (shared across worktrees); cwd persists between Bash calls, always `cd` explicitly; a full-suite result recorded under runner-heavy workers is not evidence, re-run failures alone; a 30-turn arm is a truncation of the same games, not a board class — judge deaths at full length; a refuted rule earns a paragraph beside the code, not a scaffold; string-replace edits of this file must be verified (a stale "Bot head" survived six cycles — hence this rewrite).
| worktree/branch | model | task | merge rule |
|---|---|---|---|
| contest-diag (CC) | opus | why do freed or crossing pieces die in contests `contest` did not price? Rebuild P1's rule arm from origin/beh-p1, trace the added mixed contest deaths and D1's remaining ones, find the pricing gap, propose ONE rule → docs/design/contest-gap.md (design + falsifier, no rule shipped) | doc merges as-is; a rule becomes an implementer under keep-or-revert |
| potion-v3 (CC) | opus | the collector's exposure priced at the PLAN's arrival cell (per candidate), not where it stands — the mechanism P2 and D4 both exposed; instrument-first | potions reckless share down and profitable-safe up, deaths not up, pickups no collapse; other classes byte-identical; else revert with the record |
Queued: PR #17 body refreshed 06:05Z; PR #24 body line for the claims fix + review-2 at the next TT change; checkpoint v6 (16 arms + unloaded jest + A/B vs v5 per class at 60 turns); BEHAVIOUR-AUDIT-2 classes → implementers; D5 (room saturation under a slider) if audit 2 ranks it; the deploy-hazard decision (owner); drives rebase after #17.
Ping: send_later 55 min, re-armed on each firing (current trig_01BE3Zau98sdSG4FYwH46ufe, fires 06:10Z). Lesson: read `date -u` before judging a ping late — a duplicate was armed at 06:06Z on a wrong time estimate and deleted. 05:14Z ping: four workers alive (bank-f23 in its A/B; P1 instrumented, P2 and P3 implemented and measuring); nothing to merge..

## Queue, ranked (nearness-to-fruition × value × complexity deleted)
1. Simplification audit (post-cut dead code, special cases, duplicated abstractions) → Sonnet executors.
2. Lookahead: follow 08-DEPTH-VERDICT.md's recommendation (depth preconditions done; chained depth 2 does not pay at production budgets).
3. goto weight defect (docs/design/drives/01, M1) — fix on the working branch.
4. TacticToes server/frontend simplification now the engine owns the turn.
5. Entrapment with a horizon; potion member v2 (must change WHICH pickups).
6. Drives: rebase feature/drives-preferences after #17 lands.
