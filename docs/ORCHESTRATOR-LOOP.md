# ORCHESTRATOR LOOP — living state (rewrite every cycle; this file survives compaction)

Standing orders (owner, 2026-09-03): work indefinitely; up to 8 subagents (Opus for
judgement, Sonnet for precisely-specified mechanical work); subagents report ONCE, briefly,
at the end, never mid-work; orchestrator reads minimally; checkpoint stable bot-design
states as `stable/*` branches on both repos; use self-play (deterministic runner +
`scripts/ab-compare.js`, per board class, never pooled) as the sanity gate for every
architectural change against the stable baseline, but do NOT over-optimise against it
(it lacks strategy diversity); pursue any factoring that deletes special cases in favour
of parameterised powerful abstractions; a recurring 55-minute ping keeps the cache warm;
nothing goes to `develop`/`main` directly (PRs #17 Chris-Centaur, #24 TacticToes).

## Standing orders, amended 2026-09-05 13:00Z by the owner
- Three dimensions now: (1) OPERATOR UX of the Centaur interface (research, theory, iterated designs for presenting what the bot generates; efficient, intuitive controls meeting known human preferences for game interfaces; frontend performance so the UI is as responsive as possible; SUBSTANTIAL design effort on communicating LATENCY — client↔centaur, centaur↔game server, decision deadline — even though the dev environment has none; inject latency to test) — about a third of orchestrator attention and agent budget; (2)+(3) bot intelligence, populated by the orchestrator's own creativity — two thirds.
- Maintain an average of NINE Opus agents in flight; never above twelve; below six triggers metacognition about what else is worth the resources.
- The behaviour programme on the 26-game corpus is at its floor (see the tally); new intelligence work must come from new dimensions: more board, opponent diversity, outcome-based (win/draw/loss) measurement, budget allocation, endgame, soundness classes that are free in play, search efficiency.

## Baselines
- Chris-Centaur `stable/one-engine-lens-v6` = 29d308b (97 suites / 1674 tests; 0 inversions on 16 arms; byte-identical to v5 on every class at 60 turns; carries plan-2 item 7, capof, review-2 and F3, the outcome bracket and claims re-vendors, the audit-2 instruments). Older: v5 = 1ca19d0 (97 suites / 1668 tests; 0 inversions on 16 arms; A/B vs v4 per class: mixed deaths 0.43→0.30 and edge 0, snakes/sparse byte-identical, potions 60t deaths down on 3/4 and on seeds 1–3; carries the honest contest floor, the peril-cache and horizon fixes, and the re-vendored engine with the outcome bracket). Older: v4 = bab7238, v3 = 90418c4, v2 = 439bec4, v1 = 514e1c6.
- TacticToes `stable/one-engine-v6` = 31e428e (two engine perf rounds: pawn targets once per board, computeClaims −52%; the bot ~35% less CPU per node in total; 21 suites / 358 tests; goldens byte-identical). Older: v5 = 38c60f1, v4 = 7483724. Older: v3 = a20c075, v2 = 526ff4f, v1 after the presence/grammar fix.
- Runner recordings for A/B: `node dist/tests/local-game.js <mixed|snakes|sparse|potions> 30 <seed> --nodes --json=F`; compare with `node scripts/ab-compare.js base.jsonl new.jsonl`.
- Inversion gate, 16 arms, every one must print no `INVERSION` line: `for s in mixed snakes sparse potions; do for d in 1 2 3; do CENTAUR_DEBUG_INVERSION=1 node dist/tests/local-game.js $s 30 $d --nodes; done; done` and then `for d in 4 5 6 8; do CENTAUR_DEBUG_INVERSION=1 node dist/tests/local-game.js potions 60 $d --nodes; done` — the four potion arms run to sixty turns because the two soundness classes fixed in 6e7b7d0 (a death in the optimistic timeline read as a proof; `room`'s maximised side read off a crowd containing a held cloud) fire on seeds 5 and 8 only past turn thirty, where the twelve 30-turn arms never looked.
- Opponent baseline (`--opponent`, against a NON-mirror field — see `resolveOpponent` in local-game.ts): `node dist/tests/local-game.js sum mixed,snakes,potions 30 3 --nodes --opponent=material-only --json=vs-material-only.jsonl --label=vs-material-only`; team 0's meals/100↑deaths/100 vs the seed-1-3 mirror baseline: mixed 7.78→13.17 / 0.00→1.23, snakes 18.33→18.33 / 0.00→0.00, potions 9.96→11.02 / 0.40→1.22 — a material-only field neither seeks food (weight 0) nor avoids a contest the way the default profile does, which is why team 0 eats more and dies more against it than it does mirroring itself.

## Active wave (update on every launch/merge) — 2026-09-05 17:35Z (ten agents; after the restart all workers relaunched from pushed WIP; TacticToes tt-perf merged at 29a98cd = engine 37% faster, bot 19.5% less time per node, re-vendored)
Merged this wave: ux-eval (17 findings, 10 fixed: a rail candidate click never armed Space for a snake (Direction vs cell index), __lensLastSafePressMs had no producer, the duplicate #latency-mount, hue-only clock urgency, the undo stack cleared silently at the turn boundary, two vacuous drill assertions; contrast 26/83 → 7/85 failing; keyboard 0 → 62 tab stops; dialogs and chips got roles; timed scenarios: L1 strip paints 59.5 ms after board-update, foil 0 keys, lock 2 keys/1.8 s, undo 1 key; 10 proposals ranked, P-1 = teach the harness to send controlled-snake-turn-data, P-2 two lock affordances, P-3 stageSelectedMove refuses silently), endgame (7b5adbf+488b76a: outcome instrument W/D/L + lead trajectory + `--side` swap + ab-compare outcome section; the cap read wherever a settlement has ENDED (removes an unsound floor that inverted 16,510×/game past turn 100); FINDINGS: the runner stated no maxTurns so the terminal member's share of the fold was ZERO in every measurement so far; stating it → 5,195 inversions (a terminal.ts bound defect, live in production); vs material-only side 0 loses 16/16 mixed+potions, side 1 wins 16/16 — a roster asymmetry; 6 of 9 late deaths are self-collisions in pockets = D5), ux-alerts (src/web/alerts.js at #alerts-mount: fatal-unpinned, press-window, wire-stale, wire-degraded, lock-refused, stage-drift → board-edge ring / WebAudio earcons gesture-gated / Notification when hidden / live region; cooldowns in turn budgets, ring ≥700 ms apart, ≤2 earcons per turn; scripts/alerts-drill.js 46/46; wire gaps: no pin/unpin on the wire, no producer for __lensLastSafePressMs — with ux-eval), ux-manual (d21c07d: docs/OPERATOR-MANUAL.md 494 lines with 17 screenshots; src/web/tour.js — 13 steps, keyboard-driven, never blocks the game; two defects filed to ux-eval: a duplicate #latency-mount clips the strip, nothing sets __lensLastSafePressMs so the notch never draws), ratchet-2 (room.lo 73→0 at its cause — `barsIn('lo')` admitted barriers only from units alive in our worst world, backwards for a fear: `s.worstAlive || s.bestAlive`; A/B deaths up on 0/5 classes, mixed meals +0.77; command.hi 600→65 and food.hi 63→0 measured and REVERTED for deaths — a bracket over a contingent settle cell taxes advancing when the term prices a reward and only pays when it prices a danger; reach.hi/lo refused at classification), tt-perf-2 (TT 31e428e = `stable/one-engine-v6`: computeClaims 549→251 µs; bot 0.225→0.183 ms/node; re-vendored into the bot, engine gates green; docs/ENGINE-PERF.md; the grammar's answering is what remains and VENDOR.md forbids a second encoding), budget (BUDGET.md: deaths at 0.5/1/2/4× — mixed 19/14/24/16, potions 22/14/18/22, snakes and sparse byte-identical across 8×; more budget worse on 15/24 pairs p=0.0024; 4× deaths rotate contest→bodyBlock/edge; contact is the axis; a board-only quiet set of 49% of decisions never changes; NO allocation rule; production ≈ 2× up the plateau so the deadline risk is shrinking, not growing — nodes-per-deadline work protects the margin, it does not buy play), ux-latency (a9704ef: lensRender 132→81 ms, renderFrame 24→8 ms; the ladder DISCONNECTED→STALE→DEGRADED→THINKING→LIVE with thresholds as fractions of the turn budget B — RTT warn 0.1B, DEGRADED max(150 ms, 0.3B), frame age THINKING 0.5B / DEGRADED 1B / STALE 2B; the last-safe-press notch at deadline − (RTT/2 + server work); optimistic chips ack/applied/refused; injected-latency dev mode; 13 shots; 0 differing pixels on a free wire), ux-ia (the IA: L0 depleting clock bar on the board's edge, L1 stage line + unfinished-business strip, L2 two full-size cards with bands, L3 one unmodified key away; one chip grammar glyph·verb·key·state; `bracket`/`vim`/`left hand` schemes and three densities in localStorage; undo stack, one confirm; lens-ia.test.ts 28 assertions found two live defects — Shift+,/. never fired, the pinned stage line read stale rank 1; +3600/−62), ux-secondary (fcb2bcc: page-chrome.js — one header and peripheral status chips on an Okabe–Ito palette across five screens; config's binding surface with exact upsert and undo; history deep link #turn=n into the lens; activity band viewport-sized; connection-debug's seven undefined tiles fixed; +3542/−839), docs-index (docs/design/README.md — 32 docs indexed with status; docs/design/DECISIONS.md — 16 rulings; a successor reads these before anything else), perf-1 (98770b3: 17% less time per node, byte-identical; four key-machinery folds), ux-research (docs/design/ux/01-RESEARCH.md: the turn clock as a shape on the board's edge; an unfinished-business strip; the foil promoted; two full-size moveset rows; a staleness ladder with a last-safe-press mark at deadline − RTT/2). State: bot head = the working branch tip; `stable/one-engine-lens-v6` = 29d308b (src byte-identical to it); TacticToes `stable/one-engine-v5` = 38c60f1. Both correctness reviews closed; all five simplification plans executed; the behaviour programme at its floor on the 26-game corpus (potion member CLOSED; class A accepted at this scale; weights a deaths-local-minimum). Lessons (new 17:55Z): two workers built the same colour-swap option under different names (`side` vs `deciderIndex`) — when two briefs touch the same seam, name the shared field in both briefs up front. Lessons (new 17:40Z): `cmd | tail && echo OK` reports the PIPE's exit, not cmd's — a merge with a syntax error was committed and pushed (7b5adbf, fixed forward in 488b76a); gate with `cmd > file; echo $?` or `set -o pipefail`. Lessons (new 13:55Z): the container was OOM-killed (exit 137) with nine agents running jest and runners at once — briefs now say `npx jest --maxWorkers=2` and one runner game at a time; the tt-perf agent edited the main checkout's src/engine-vendor despite the brief and my `commit -a` swept it into 80d2602 (content correct = TacticToes 29a98cd; vendor-sync test green) — never `commit -a` in the main checkout while agents run, `git add` the loop file by path. Lessons list: see the previous revision of this file in git history (commit a33bae0..) — unchanged: no `git stash`; checkpoint+push every step; relaunch dead agents; Sonnet briefs forbid background runs; verify selective merges with `git diff --stat HEAD~1 HEAD`; verify string edits of this file; read `date -u` before judging a ping late; a refuted rule earns a paragraph, not a scaffold; judge deaths at full length per class.
| worktree/branch | model | dimension | task | merge rule |
|---|---|---|---|---|
| tt-perf (TT) | opus | bot | the engine's `pawnTargetsOf` rebuilt per `legalActions` call is 17.8% of the bot's process: compute it once per board (memo keyed on the board shape, inside the vendored module's rules), enumeration-gated; re-vendor after merge | goldens byte-identical; settlePartial enumeration green; engineVendor compile; measured speed-up |
| ordering (CC) | opus | bot | search efficiency: measure how often the final choice was the first/second candidate per decision class, where the search spends nodes on candidates it never picks; ONE ordering rule (from cheap readings already computed) gated at equal node budget: deaths not up on any class, decision-change count reported → docs/design/ORDERING.md | at equal nodes: deaths not up, ideally the 4× decisions reached at 1×; 16 arms zero; determinism re-pin only with the transcript read |
| ux-perf-2 (CC) | opus | UX | long-session frontend health: heap and DOM growth over 200 turns with every module live (lens, latency, alerts, tour, page-chrome), listener and timer leaks, script load order and size, first-paint on a cold load, a 4-hour soak simulated fast; fixes gated byte-identical on the walkthrough → docs/design/ux/08-LONG-SESSION.md | lens gates; walkthrough pixel diff; heap flat over the soak |
| ux-fixes (CC) | opus | UX | ux-eval's ranked proposals P-1..P-10 in order: the harness sends controlled-snake-turn-data so drills exercise the real staging path; one lock affordance; stageSelectedMove's refusal surfaced; then the rest as far as they go | lens gates; walkthrough + all drills; no decision moved |
| ux-review (CC) | opus | UX | post-game review mode in history.html + src/web/review.js: step a finished game through the lens replay, an automatic index of the moments that mattered (deaths, material swings, lead changes, operator interventions), a "why did it do that" reading from the breakdown and conditional ranking, bookmarks as deep links, keyboard j/k, export of a turn as a shareable link → docs/design/ux/07-REVIEW.md | lens gates; a review drill under Playwright; no live-page files touched |
| wide-corpus (CC) | opus | bot | more board: a wide corpus (twenty seeds per class + larger/denser/asymmetric scenarios), baseline recorded, behaviour audit 3 on it → docs/design/BEHAVIOUR-AUDIT-3.md + scripts | doc + scenarios merge; classes → implementers |
| opponents (CC) | opus | bot | REPORTED (d9b8fea): six profiles (aggressive, territorial, cautious, glutton, random-legal, material-only); 336-game round robin; no fixed table beats the default outright; weakness class = glutton on potions (our deaths +3/+7 while theirs stay flat: our contested-meal licence spent against a player that will not give way); snakes mirror baseline inflated by 7 self-collisions. Now RECONCILING its `deciderIndex`/`--decider` colour swap onto the merged `side`/`--side` before merge | five old scenarios byte-identical side 0; alias proven on one round-robin cell |
| deadline (CC) | opus | bot | production deadline robustness: play at 0.25× and 0.125× budget; the anytime property (a legal, non-suicidal answer at ANY cutoff, incl. the first slice); the wall-clock path under simulated late `endTime`, jitter and a shrinking reserve; what the kernel returns when the deadline is already past; a `--deadline-ms` runner mode if none exists → docs/design/DEADLINE.md; fixes only for a hole (no answer / an illegal or fatal fallback) | tsc, eslint, kernel suites, determinism unchanged at 1×; holes fixed with a regression test |
| deep-deaths (CC) | opus | bot | why does MORE search kill? Reproduce the 4× deaths BUDGET.md found (bodyBlock/edge, contact ≥3, one unit's cell changed), trace which refinement step (rung, joint plan, polish) introduced the fatal plan and which member priced it above the 1× choice; is it an evaluator blind spot the search exploits (own bodies on joint plans, edge cells) or a floor/est bias at depth? → docs/design/DEEP-DEATHS.md with ONE rule and a falsifier | doc merges as-is; a rule under keep-or-revert at 1× AND 4× (deaths not up at either) |
| terminal-sound (CC) | opus | bot | the terminal member's bounds invert when the cap is stated (5,195 inversions on the 30-turn arms: `clampTo` replaces rather than meets; `finish`'s min/max promotes an interior ceiling to a floor) — production DOES state the cap (translate.ts copies setup.maxTurns), so this is live: fix the bound algebra, then make the runner state the cap like production, re-baseline, A/B per class | 16 arms zero WITH the cap stated; exact-reply exact; deaths not up per class vs the capless head; re-pins only with transcripts read |
| side-asym (CC) | opus | bot | vs material-only side 0 loses 16/16 mixed+potions from turn 10, side 1 wins 16/16: roster/spawn asymmetry or a runner/engine side bias? Make the standing corpus honest (symmetric rosters or both-sided arms in every A/B) → docs/design/SIDE-ASYMMETRY.md | doc + runner change; five old scenarios' side-0 play byte-identical |
Queued: checkpoint v7 (room.lo repair moved mixed play: 16 arms + unloaded jest + A/B vs v6 at 60 turns) at a quiet ping; PR #17 and #24 body refresh (UX wave, perf rounds, budget null, v6 checkpoints) at the next quiet ping; ratchet-2 (close the largest per-feature law-sweep classes at their cause if free in play; after perf-1 releases src/lobster); ux-controls preferences persistence; TacticToes lobby item 4 (owner); drives rebase after #17; PR bodies after this wave.
Ping: send_later 55 min, re-armed on each firing (current: armed 16:47Z for 17:42Z). 16:46Z ping: nine workers alive; nothing to merge. 13:42Z ping: eight workers alive (ux-ia, ux-latency, wide-corpus, opponents, budget, endgame, ratchet-2, tt-perf); nothing to merge.

## Queue, ranked (nearness-to-fruition × value × complexity deleted)
1. Simplification audit (post-cut dead code, special cases, duplicated abstractions) → Sonnet executors.
2. Lookahead: follow 08-DEPTH-VERDICT.md's recommendation (depth preconditions done; chained depth 2 does not pay at production budgets).
3. goto weight defect (docs/design/drives/01, M1) — fix on the working branch.
4. TacticToes server/frontend simplification now the engine owns the turn.
5. Entrapment with a horizon; potion member v2 (must change WHICH pickups).
6. Drives: rebase feature/drives-preferences after #17 lands.
