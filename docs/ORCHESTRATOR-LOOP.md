# ORCHESTRATOR LOOP — living state (rewrite every cycle; this file survives compaction)

Standing orders (owner, 2026-09-03): work indefinitely; up to 8 subagents (Opus for
judgement, Sonnet for precisely-specified mechanical work); subagents report ONCE, briefly,
at the end, never mid-work; orchestrator reads minimally; checkpoint stable bot-design
states as `stable/*` branches on both repos; use self-play (deterministic runner +
`scripts/ab-compare.js`, per board class, never pooled) as the sanity gate for every
architectural change against the stable baseline, but do NOT over-optimise against it
(it lacks strategy diversity); pursue any factoring that deletes special cases in favour
of parameterised powerful abstractions; a recurring 55-minute ping keeps the cache warm;
nothing goes to `develop`/`main` directly (PRs #17 Chris-Centaur, #24 TacticToes).

## Baselines
- Chris-Centaur `stable/one-engine-lens-v5` = 1ca19d0 (97 suites / 1668 tests; 0 inversions on 16 arms; A/B vs v4 per class: mixed deaths 0.43→0.30 and edge 0, snakes/sparse byte-identical, potions 60t deaths down on 3/4 and on seeds 1–3; carries the honest contest floor, the peril-cache and horizon fixes, and the re-vendored engine with the outcome bracket). Older: v4 = bab7238, v3 = 90418c4, v2 = 439bec4, v1 = 514e1c6.
- TacticToes `stable/one-engine-v4` = 7483724 (review fixes + outcome bracket; 21 suites / 356 tests; goldens byte-identical). Older: v3 = a20c075, v2 = 526ff4f, v1 after the presence/grammar fix.
- Runner recordings for A/B: `node dist/tests/local-game.js <mixed|snakes|sparse|potions> 30 <seed> --nodes --json=F`; compare with `node scripts/ab-compare.js base.jsonl new.jsonl`.
- Inversion gate, 16 arms, every one must print no `INVERSION` line: `for s in mixed snakes sparse potions; do for d in 1 2 3; do CENTAUR_DEBUG_INVERSION=1 node dist/tests/local-game.js $s 30 $d --nodes; done; done` and then `for d in 4 5 6 8; do CENTAUR_DEBUG_INVERSION=1 node dist/tests/local-game.js potions 60 $d --nodes; done` — the four potion arms run to sixty turns because the two soundness classes fixed in 6e7b7d0 (a death in the optimistic timeline read as a proof; `room`'s maximised side read off a crowd containing a held cloud) fire on seeds 5 and 8 only past turn thirty, where the twelve 30-turn arms never looked.
- Opponent baseline (`--opponent`, against a NON-mirror field — see `resolveOpponent` in local-game.ts): `node dist/tests/local-game.js sum mixed,snakes,potions 30 3 --nodes --opponent=material-only --json=vs-material-only.jsonl --label=vs-material-only`; team 0's meals/100↑deaths/100 vs the seed-1-3 mirror baseline: mixed 7.78→13.17 / 0.00→1.23, snakes 18.33→18.33 / 0.00→0.00, potions 9.96→11.02 / 0.40→1.22 — a material-only field neither seeks food (weight 0) nor avoids a contest the way the default profile does, which is why team 0 eats more and dies more against it than it does mirroring itself.

## Active wave (update on every launch/merge) — 2026-09-05 04:35Z
Merged this cycle: lens audit (8853d9b: replay drawn from the record not the live source; reservoir rows found under the cluster key nothing read; four empty states told apart; `lo`/`⌈w⌉`/Q named; dead transport seam gone; 18 findings in decision-lens/09-AUDIT.md), TacticToes plan-2 items 1–3, 5–9 (a20c075 = `stable/one-engine-v3`; item 4 left for the owner: no automated gate; E-a deferred), engine re-vendored for E-b (fe259cd), PR #24 body refreshed. Then plan-3 items 1, 7, 10 (083c1f0: 17 suites on the shared fixtures, −367/+105; full suite 94/1616 green, 0 skipped) and item 2 (fce8f47: one WriteQueue under both loggers, batch steps verbatim). Item 7 leftovers in turn-oracle.ts/staging-legality.ts/substrate.ts (healthAfterEntering, projectedHealthCost, destCoordOf, noOrderCandidate) skipped for ownership → queued. Then plan-3 items 4, 8, 5 (0bb68b2: settleInputBase/claimsAfter, perBoard hold-outs, GenerationRig; byte-identical runner all-zero after each; −165/+111). Then item 7's rest (a038c05: −49/+2). Plan 3 now has 1, 2, 3*, 4, 5, 7, 8, 10, 12* done or in flight (* in lens-2); left: 6, 11 (after b1-sound), 9 (p3-d). Then item 9 (db3b166: one consider step; all-zero runner diff). Full jest on fce8f47 under heavy contention had 3 failures (staging-safety, lobster-trio, basic-intelligence at 2290 s); the same three pass on db3b166 (65 tests): wall-clock starvation, not the write queue. Rule: never trust a full-suite result recorded while runner-heavy workers are up; re-run the failures alone. Then the behaviour audit (6bfccc3: D1 contest blind to the occupied cell = every edge death; D2 pawn orientation invisible → parking; D3 room's fear falls with length; D4 potion peril far horizons constant; D5 room saturates under a slider; D6 runner counts rotation as a move; the foodEnergy scenario gap). Then lens-2 (a417e05: breakdown row emitted and carried, BREAKDOWN panel renders live and in replay; seven web items; arrived frame on join; plan-3 items 3 and 12; 21 lens suites / 292 tests). Plan 3: only 6 and 11 left (after b1-sound). Then D4 refuted (geometric horizon weights λ=1/4: reckless 59→79%, profitable-safe 20→8% on 7/7 potions seeds; reverted; negative result in potions.md; the 172-line fixture test and test-only export NOT taken — a refuted rule earns a paragraph, not a scaffold). Then b1-oracle (8181233: exact-reply oracle, seed-1 arms in the default suite (+43 s), all sixteen under `npm run gate:exact` (231 s); law-sweep ratchet pinned at command.hi 600, reach.lo 128, reach.hi 220, room.lo 73, food.hi 63, contest.lo 30, momentum.lo 27, energy.hi 10, material.hi 8, totalLo 0; §7/§7.1 rewritten; `origin/b1-sound` kept as the record of the declined repairs). PLAN-2 item 7 (B1/B3 fold) is now unblocked. Then lens-walk (4783bf3: `src/tests/lens-walkthrough-server.ts` + `scripts/lens-walkthrough.js` drive a real game into play-game.html under Playwright; 14 live defects fixed incl. live clients stuck in SCRUBBED read-only, replay's 0×0 board, multi-unit locks refused, widen banner never shown; 32 PNGs (2.3 MB) in decision-lens/walkthrough/, 10-WALKTHROUGH.md; 8 open items O1–O8 → lens-3). Then beh-contest (60fe8f1: D1's first shape — origin cell in enemyArrivals, graded p_e(c) — reverted: edge deaths 3→0 but potions deaths 26→28 and mixed meals −13%; diagnosis: grading divides charges by |actions| and costs contest ~¾ of its seated strength; next shape 1−ε+ε·p. Kept: enemyOccupiedEntriesLost counter, D6 (parked share read off the cell held: mixed 2.5→7.2%, potions 4.2→10.4%, longestPark), meals/grownMeals split, `--food-energy=N`, `sparse-lean` arm (foodEnergy 20; grown/meals 0.84), contest-occupied-cell.test.ts pins reproduction A as the defect, potions.md's stale 875/103 corrected). Bot head 60fe8f1; `stable/one-engine-lens-v3` = 90418c4.
Lessons: the SESSION rate limit (HTTP 429, resets on the hour) kills every Opus worker at once (00:2xZ: three lost, all with pushed checkpoints; WIP committed by the orchestrator); do not relaunch before the reset; container restarts kill every worker (22:40Z: three workers lost, all had pushed checkpoints, relaunched with continuation briefs at 22:50Z — the checkpoint-and-push rule is what made that cheap); a dead agent cannot be resumed by SendMessage, relaunch; Sonnet stalls on background jest EVERY time the brief only says "foreground": briefs must say "never pass run_in_background, never call Monitor, never wait for a notification", resume with that line when it happens; NO `git stash` anywhere (shared across worktrees); cwd persists between Bash calls, always `cd` explicitly.
| worktree/branch | model | task | merge rule |
|---|---|---|---|
| beh-audit-2 (CC) | opus | second behaviour audit on the SAME corpus as the first (D1 landed): what moved, what is still wrong, new classes ranked; docs/design/BEHAVIOUR-AUDIT-2.md | doc merges as-is; classes become implementers under the keep-or-revert rule |
| tt-review-2 (TT) | opus | TacticToes REVIEW-1 findings 2–7 to a verdict each (claim tier ceiling vs simultaneous collectors; food consumed by a doomed eater; three placement issues; deploy hazard as a recommendation) | functions suite; goldens byte-identical; enumeration for engine items |
| bank-f23 (CC) | opus | REVIEW-1 F2 (a B1/B3 view's peril read on its own board) and F3 (the entanglement gate reads base) | failing tests first; gate:exact 16/16; 16 arms zero; A/B per class, deaths not up |
Queued: PR #17/#24 body refresh after capof/item-7/review-2 land; the deploy-hazard decision (owner); D5 (room saturation under a slider); and PLAN-2 item 7 after b1-sound and p3-c; full jest + 16 arms on the head after p3-* merge, then checkpoint v4; PR #17 body refresh after plan 3 lands; TacticToes: plan-2 item 4 (owner), a third audit only if the second's "not worth it" list changes; drives rebase after #17.
Ping: send_later 55 min, re-armed on each firing (current trig_018Zcrf1ZPoLpq3LqXsobUUf, fires 05:14Z). 04:17Z ping: review-2 at F10 (finishing), beh-audit-2 and tt-review-2 starting; nothing to merge.. 02:23Z ping: review-1 at a284186 (a sibling-perspective fix re-pinned the lens cost counters), tt-outcome starting, 16-arm gate on the re-vendored head zero on all 16; nothing to merge. 01:16Z ping: d1-three relaunched after the rate-limit reset; checkpoint-v4 gates (16 arms + full jest) started on bab7238 in the background (scratchpad v4gate.sh). 00:20Z ping: three workers alive (p3-f on the D2 dose sweep; lens-4 has rankConditional ranking the rest of the cluster at 5c4888d, proving; d1-three has the honest floor at 491a6eb, measuring), nothing to merge. . 23:24Z ping: three workers alive (p3-f merged the working branch and is measuring D2; lens-3 re-driving the walk with a tentative pin; d1-two has the lightened charge implemented at c577c4e and is measuring), nothing to merge.. 22:18Z ping: p3-f at e2442c0 (items 6, 11 committed; D2 next), lens-3 at 5e70085 (O5 done), d1-two launched; the beh-contest merge is confirmed (5 suites / 47 tests green on a104f36)..

## Queue, ranked (nearness-to-fruition × value × complexity deleted)
1. Simplification audit (post-cut dead code, special cases, duplicated abstractions) → Sonnet executors.
2. Lookahead: follow 08-DEPTH-VERDICT.md's recommendation (depth preconditions done; chained depth 2 does not pay at production budgets).
3. goto weight defect (docs/design/drives/01, M1) — fix on the working branch.
4. TacticToes server/frontend simplification now the engine owns the turn.
5. Entrapment with a horizon; potion member v2 (must change WHICH pickups).
6. Drives: rebase feature/drives-preferences after #17 lands.
