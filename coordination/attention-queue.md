<!-- SNAPSHOT: source scratchpad/attention-queue.md — synced 2026-09-01T01:13:23Z by the branch-topology housekeeping task.
     This is a point-in-time copy, not the live document. The working copy is the coordinator's
     scratchpad; this branch exists so the owner can reach it if that box is unreachable. -->

00. OWNER DECISION: batch-2 size on the local box — as-shipped 2,472
   games (~1.4 nights; P11 cannot decide the branch merge either way) vs
   P11 at full power 5,208 (~2.9 nights) vs + resized piece cells 7,560
   (~4.1 nights). Specs annotated; generator regenerates any option.
0y. Lane-(a) next increment: dynamic within-bot selection among
   collection members (SlateId single-member today). Also: P11 baseline
   arm has no A/A floor of its own (second baseline null = cheapest
   addition if local box has time).
0z. NEW: verify-soak-races flaky under full-suite load at depth tip
   (2 fails, passes isolated) — depth follow-up item. Dodge caveats: the
   corpus is hazard-free + all pickups 1-step; hazard boards and 3-turn
   reaches unmeasured (batch-2 material).
0a. OWNER DECISION PENDING: depth+enumeration default-on now (unpromoted,
   live sweep owed) vs default-off config until a paired live sweep
   promotes them. Distiller P1.
0b. Design candidate: narrow belief-staging to rows the deep search
   actually informed (distiller P3).
# Attention queue (priority order; dribble-feed per directive 3)
0. NEXT BUILDS once depth lands (teardown DONE @ a4f1706):
   royal-margin correction (27% of king deaths own-team-inflicted;
   deliberately deferred one-change-per-commit);
   opponent-model socket (biggest re-carving; ALSO unblocks potion-seek's
   exposure half — 99.6% false-alarm needs a contest model); fog-machinery kept CROSS-TURN capable (invisibility potions coming; ruling 20) — re-scope only its per-turn reset to
   staged-moves-only; weight-profile-per-bot consolidation; potion
   arm-collect-spend commitment object. Methodology: every experiment
   must declare maxTurns (metric character flips without it).
# Attention queue (priority order; dribble-feed per directive 3)
1. DELIVERED: depth diagnosis; scout fixes landed (wrong-line bug,
   silent-gate marker, P11 spec corrected). NEW: argmaxMoved was false on
   every measured ply → all prior scout advisory-rate/parking telemetry
   suspect; re-read before relying on it. Kit has 3 pre-existing
   partial-engine-vendor-sync failures (not from these changes) — needs a
   look before batch 2.
   (was: depth diagnosis.) Headlines: the one-unit cap NEVER fired
   (not the bottleneck); scout is a silent no-op without cluster-enum
   (P11 as specced would blame the scout for a null — fix launched);
   3 of 4 deep-search prerequisites already built inside the scout;
   scout follows the wrong line from ply 2 (bug; fix launched). Real
   depth build rides the redesign's aggregation socket.
2. DELIVERED: core redesign doc + increment 1 BUILT (registry + belief,
   byte-identical). Next increments when ready: cap-vs-merge as first
   entry comparison; shadow-driven scheduling. New open items: anytime
   loop slice-count nondeterminism (clocks faked, output identical —
   instrument mystery); scout import-ban lint rule shadowed
   (pre-existing). Owner decision
   points if asked: (a) two safety flags reclassified from strategy
   candidates to inviolate kernel; (b) cohort ladder retires, round-fusion
   machinery survives; (c) build order registry-first, byte-identical.
3. DELIVERED: re-score. No flips; P7 strengthens (77%→21% weight share);
   P3 weakens + sits on unfloored cell (which floor is right decides it —
   candidate methodology item for batch 2). sharePar ~2× noisier: ~3×
   games for equal power. Winner-take-all line was the Elo ladder input.
4. DELIVERED: portfolio design + slider-attack retrodiction (105/105
   precision when visible; cautious channel 0/73 false; 4µs/move; all
   severs occur in potion-ON games). NEXT CASES queued: defensive mirror
   (our snakes cut 201× unwatched); simultaneous-movement blindness (42%)
   is a depth/aggregation problem, feeds redesign. Sever denominator
   corrected 1,167→460 cells for this corpus. OPEN OWNER QUESTION: is the
   potion toggle (invulnerabilityPotionEnabled, default false) ON in real
   games? Blocks the potion spec, nothing else. Build rec: ray-crossing
   primitive + slider attack vector first, retrodiction-validated.
   Redesign-build precondition recorded: bank must keep per-feature parts
   (not folded totals) or the live-dial latency promise fails.
5. DONE: sharePar + mutual-wipe fixes landed; batch-3 file reverted.
   Follow-ons queued: (a) re-denominate evaluator material term as share
   (large change — fold into redesign build); (b) sharePar null floors
   don't exist yet — any sharePar-quoted treatment needs its own A/A
   floor; (c) pre-existing aggregate.js --base crash on missing arm.
6. Batch 2 held until architecture+methodology work lands (owner ruling).
7. Harness defect: empty squad metadata blocks own-team detector checks.
8. BATCH1-REPLAY-DEFERRED ingest --write still pending deliberately.
9. Lobster Ledger page: needs WASM removal, metric correction, potion
   endogeneity correction (several §10 claims now stale).
