{
 "_comment": [
  "P11 — CENTAUR_SCOUT (CL6a), status probe-passed.",
  "",
  "QUESTION: At 2000 ms, does advisory depth buy placement, and what does 16.8 ms/decision cost the one-ply search that pays for it?",
  "",
  "ARMS: enum-on  |  enum-on+scout-observe  |  enum-on+scout-advise",
  "READS OUT: scoutThreads, scoutPlies, postContactPlies, scoutRefusals, worstWallMs, decisions, score",
  "",
  "DESIGN NOTE: The BASE arm must be enum-on, not off, and EVERY contender carries CENTAUR_CLUSTER_ENUM=on: `scout.run` has exactly one call site and it is inside `openCluster`, below the cluster-enumeration gate, because the threads' seeds are the enumeration's own proposals. CENTAUR_SCOUT alone is a silent no-op, so an off/observe/advise triple with the enumeration off is three IDENTICAL builds and its null is a null about the harness filed against the flag. Measured: enum off -> threads=0 plies=0 findings=0; enum on -> threads=7 plies=21 findings=3, same board. Since CL6a's repair the scout report also carries `gatedBy`, which names the gate when the runner was never reached — read it on every arm before reading anything else. Same shape as P10, and for the same reason. Within that base, observe AND advise as separate contenders: observe measures the cost with no behaviour change at all, so observe-vs-base isolates the tithe and advise-vs-observe isolates the advice. One contender carrying both would report their sum. Potion-bearing boards are REFUSED by the door under a potion-free premise and are counted as refusals — expect nonzero scoutRefusals on potion cells, and read it as the honest refusal it is.",
  "",
  "REQUIRES: CENTAUR_CLUSTER_ENUM=on on every contender, including the base. Verify from the RESOLVED flag stamp on the per-game rows and from ScoutReport.gatedBy, not from the environment.",
  "",
  "FLAG-VALUE TRAP: every CL flag parses only `1`, `on` or `true`, with no",
  "warning on anything else. Set the OFF arm by OMITTING the variable, and",
  "check the manifest envAtRun block afterwards. Since the CL7 telemetry",
  "closure the per-game rows also carry the RESOLVED flag stamp, which is",
  "the value that actually ran — read that, not the environment.",
  "",
  "Generated from tools/learnloop/promotion-ledger.json. Do not hand-edit;",
  "re-run make-promotion-batch.js."
 ],
 "sweepId": "p11-scout",
 "bots": [
  "lobster-territory",
  "lobster-material",
  "reflex"
 ],
 "seeds": [
  43301,
  43302,
  43303,
  43304,
  43305,
  43306,
  43307,
  43308,
  43309,
  43310,
  43311,
  43312,
  43313,
  43314,
  43315,
  43316
 ],
 "rotateSeats": true,
 "cells": [
  {
   "cell": "headline-mix-king",
   "config": {
    "name": "headline-mix-king",
    "size": 25,
    "teams": [
     "red",
     "blue",
     "green"
    ],
    "roster": [
     "king",
     "queen",
     "rook",
     "knight",
     "snake",
     "snake"
    ],
    "budgetMs": 2000,
    "turnCap": 120,
    "food": {
     "initial": 6,
     "spawnRate": 0.5
    },
    "hazards": {
     "layout": "none"
    },
    "potions": {
     "enabled": false
    }
   }
  },
  {
   "cell": "hazard-mix-king",
   "config": {
    "name": "hazard-mix-king",
    "size": 25,
    "teams": [
     "red",
     "blue",
     "green"
    ],
    "roster": [
     "king",
     "queen",
     "rook",
     "knight",
     "snake",
     "snake"
    ],
    "budgetMs": 2000,
    "turnCap": 120,
    "food": {
     "initial": 6,
     "spawnRate": 0.5
    },
    "hazards": {
     "layout": "cross",
     "damageRatio": 0.15
    },
    "potions": {
     "enabled": false
    }
   }
  },
  {
   "cell": "null-snake6",
   "config": {
    "name": "null-snake6",
    "size": 25,
    "teams": [
     "red",
     "blue",
     "green"
    ],
    "roster": [
     "snake",
     "snake",
     "snake",
     "snake",
     "snake",
     "snake"
    ],
    "budgetMs": 2000,
    "turnCap": 120,
    "food": {
     "initial": 6,
     "spawnRate": 0.5
    },
    "hazards": {
     "layout": "none"
    },
    "potions": {
     "enabled": false
    }
   }
  }
 ]
}
