# STRAGGLERS FROM THE PER-LENS ROLLUP — status against my section

Section C of `docs/design/prior-art/19-per-lens-rollup.md` @ b58a473. Recording disposition so
nothing is silently dropped.

| item | disposition |
|---|---|
| **[+] Per-unit accounts = KataGo's ownership head**, with board-area normalisation `w_o = 1.5/b²` ≈ our `K/W` | **Accepted, and it supplies an argument I had not made.** Their point that an ownership target gives *localised* credit assignment from few samples is the direct answer to ruling 49's low-density worry, and it is better than anything in my prior-art memo. Independent convergence down to the normalisation is worth recording as evidence for the decomposition. |
| **[M] Promote per-unit flows to standing telemetry** | **Endorsed, and I would rank it first of the M-items.** My own extraction turns 144 games into ~55k unit-rows at zero play cost; making that standing is the cheapest possible response to low config density, and every instrument I built this session would have been cheaper with it. |
| **[C] The fold is fine; `partitionOf` is the un-incrementalisable object** | **Agreed and consistent with my NNUE section.** `(K,W,p)` is a per-turn constant, so the fold is *more* incrementalisable than the shipped evaluator; the dense object is `partitionOf`, a whole-board set-cover where one unit's plan alters every unit's count. This sharpens my §4: `feature/commit-scope` rests on cluster/reading-granularity invalidation, not per-term incrementality. |
| **[M] `room` should count EDGES, not cells** — a1k0n's Tron fit `K₁(N₁−N₂)+K₂(E₁−E₂)`, K₁≈0.055, K₂≈0.194 | **Accepted as the most interesting open item for my lens**, because it says the *quantity* is wrong, not the coefficient — a second independent correction to the same term. Directly reproducible on our archive with `m3-instrument.py`'s first-arrival machinery (add an edge count beside the cell count and compare which predicts survival). **Not yet run.** |
| **[M] Checkerboard parity bound** | Plausible and free, with one precondition I would check first: the bipartite argument is a *trail-unit* bound, and our knight jumps (which change colour) and sliders (which cross many cells in one move) may break it. Belongs in the bounds bank, not an evaluator. |
| **[M] Articulation-point chamber tree** (12–1 in the ancestor game) | Noted; a real build, not a measurement. It is the shape the `room` family is missing, and it should be sequenced after the edge-count correction since both touch the same term. |
| **[C] Every headline number is a non-invariant aggregate over a redundant population** | **Taken, and applied to my own headline.** `POP-1` §3b re-weights the VBS gap (uniform / game-count / √n / leave-one-out / bootstrap) and **softens my verdict** from "below its floor" to "no evidence it clears its floor". Balduzzi's axiom is the reason I ran that check. |
| **[M] mElo latent-skill decomposition** | **Subsumed by a stronger negative I measured.** mElo decomposes into transitive + low-rank *cyclic* components, and `POP-2` shows cyclicity is **identically zero by construction** in this archive (three-seat games telescope). With no cyclic component mElo reduces to the transitive ratings I already report, so it cannot surface the dead knight cell here. It becomes available only under a design that can express intransitivity at all. |
| **[M] Centaur surface on FLOWS, not the aggregate; do not sum flows before caching** | **Accepted and consequential for my own artefacts**: my `Contribution{unit, flow, side, rate, horizon}` is the causal vocabulary, and summing before caching destroys exactly the per-unit attribution that makes it explainable. Worth a line in the manifest, since the natural implementation is to cache the total. |
| **[M] Risk budget as one more account**, denominated `realised share − floor share` | **Accepted; it is a better answer than my γ.** γ is a free risk-preference exponent with no derivation; a risk *budget* in share units is an account like any other, needs no new currency, and is spendable and auditable. I would drop γ in favour of it and say so. |
| **[M] My M2/M3 instrument has four consumers** | Noted; consistent with having built it. The spread-by-unit-class output is already in `m3-instrument.py` and its per-class split is what the other three consumers need. |
| **[?] Measure the error on what each admission filter REMOVES** | **Run for the slider cap** — see `M3-admitted-set-instrument.md` §6. My hypothesis (the cap removes long moves) was **refuted** by the food discriminator; the corrected finding is that the cap preserves nameable value and discards positional value. **Three filters remain unmeasured.** |
